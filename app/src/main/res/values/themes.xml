<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RetailHub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textAppearance">@style/MainTextViewStyle</item>
        <item name="android:textViewStyle">@style/MainTextViewStyle</item>
    </style>

    <style name="Theme.RetailHub" parent="Base.Theme.RetailHub" />
</resources>