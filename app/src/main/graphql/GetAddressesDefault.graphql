query getAddressesDefaultId($id: ID!) {
    customer(id: $id) {
        defaultAddress {
            id
            address1
            address2
            city
            country
            phone
            name
        }
    }
}
