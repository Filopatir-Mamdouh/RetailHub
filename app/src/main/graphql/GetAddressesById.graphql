query getAddressesById($id: ID!) {
    customer(id: $id) {
        addresses {
            id
            address1
            address2
            city
            country
            phone
            name
        }
    }
}
