mutation updateCustomerAddresses($input:
CustomerInput!) {
    customerUpdate(input: $input) {
        customer {
            addresses{
                id
                address1
                address2
            }
        }
        userErrors {
            message
            field
        }
    }
}