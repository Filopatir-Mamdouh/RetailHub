mutation updateCustomerAddresses($input:
CustomerInput!) {
    customerUpdate(input: $input) {
        customer {
            addresses{
                id
            }
        }
        userErrors {
            message
            field
        }
    }
}